import 'package:clini_app/utils/utils.dart';
import 'package:flutter/material.dart';

enum CheckBoxType { UN_CHECKED, CHECKED }

final appLocale = EnumValues({
  const Locale('en'): 'ENGLISH',
  const Locale('es'): 'SPANISH',
});
